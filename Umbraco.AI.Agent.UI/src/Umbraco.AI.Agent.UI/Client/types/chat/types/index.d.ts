/**
 * Shared domain types for chat features.
 * Re-exported from @umbraco-ai/agent for convenience.
 */
export type { UaiChatMessage, UaiToolCallStatus, UaiToolCallInfo, UaiInterruptInfo, UaiInterruptOption, UaiAgentState, } from "@umbraco-ai/agent";
/**
 * Agent item for agent selector in chat surfaces.
 */
export interface UaiAgentItem {
    id: string;
    name: string;
    alias: string;
}
//# sourceMappingURL=index.d.ts.map