// Context picker is a reusable component - no manifests needed.
// It's registered as a custom element and used directly in templates.
export const contextPickerManifests: Array<UmbExtensionManifest> = [];
