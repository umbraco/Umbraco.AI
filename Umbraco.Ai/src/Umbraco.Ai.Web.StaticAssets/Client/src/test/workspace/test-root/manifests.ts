import type { UmbExtensionManifest } from "@umbraco-cms/backoffice/extension-registry";

// TODO: Add test-root workspace manifests when needed
// This is the workspace that shows the list of tests before drilling down to a specific test

export const manifests: Array<UmbExtensionManifest> = [];
